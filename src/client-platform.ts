export enum ClientPlatform {
  IOS = 'ios',
  WEB = 'web',
  ANDROID = 'android',
  UNKNOWN = 'unknown',
}

export const ClientPlatforms = [
  ClientPlatform.IOS,
  ClientPlatform.WEB,
  ClientPlatform.ANDROID,
  ClientPlatform.UNKNOWN,
];
